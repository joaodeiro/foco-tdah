{
  "name": "kairos",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "web": "pnpm --filter @kairos/web dev",
    "web:build": "pnpm --filter @kairos/web build",
    "mobile": "pnpm --filter @kairos/mobile start",
    "mobile:ios": "pnpm --filter @kairos/mobile ios",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  }
}
